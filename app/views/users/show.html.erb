<div class="heading">
  <h2><%= @user.name %></h2>
</div>

<section class="sort-by">Sort by:
  <a href="<%= user_path(@user, sort: "newest") %> ">Newest</a>
  <a href="<%= user_path(@user, sort: "oldest") %>">Oldest</a>
  <a href="<%= user_path(@user, sort: "highest") %>">Highest Rating</a>
  <a href="<%= user_path(@user, sort: "lowest") %>">Lowest Rating</a>
</section>

<section class="user-reviews">
<% @reviews.each do |review| %>
<div class="user-review" id="review-<%= review.id %>">
  <div class="card" style="width: 18rem;">
    <img src="<%= review.book.image %>" class="card-img-top" alt="<%= review.book.title %> Cover">
    <h4 style="padding:5px;"><%= link_to review.book.title, book_path(review.book) %></h4>
    <p style="padding:5px;">Posted on: <%= review.date %></p>
    <div class="card-body">
      <h5 class="card-title"><%= review.title %></h5>
      <p class="card-text">Rating: <%= review.rating %></p>
      <p class="card-text">Review: <%= review.text %></p>
      <%= link_to "Delete Review", book_review_path(review), method: :delete, data: {confirm: "Are you sure you want to delete the review?"}, class: "btn btn-danger" %>
    </div>
  </div>
</div>
<% end %>
</section>
